---
description: SpawnWP 0.5.37 release notes — stable Cockpit first paint.
---

# SpawnWP 0.5.37

All Cockpit pages now share an early asset bootstrap that prevents an unstyled shell from being
painted while `cockpit.css` loads. A bounded fallback keeps the interface usable if an asset
request fails.
